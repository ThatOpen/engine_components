name: Shell Commands
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Run all tests
        run: yarn test